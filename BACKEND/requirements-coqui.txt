# Optional: local monolith on Python 3.11 only (not used on Render)
# pip install -r requirements.txt -r requirements-coqui.txt
TTS==0.22.0
transformers>=4.33.0,<4.47.0
torch>=2.1.0,<2.6.0
torchaudio>=2.1.0,<2.5.0
