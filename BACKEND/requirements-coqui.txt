# Pin after: pip install -r requirements.txt && pip install TTS torch torchaudio
# Use only inside project_311 (Python 3.11)
transformers>=4.33.0,<4.47.0
numpy>=1.24,<2.0
torch>=2.1,<2.6
torchaudio>=2.1,<2.5
TTS>=0.22.0
