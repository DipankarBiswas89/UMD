# Production (Vercel): point to your Render backend (no trailing slash)
VITE_API_URL=https://your-backend.onrender.com

# Local dev: leave VITE_API_URL unset — Vite proxies /api → http://127.0.0.1:8000
